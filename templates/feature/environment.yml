run_mode: isolated
max_node_retries: 1
max_gate_retries: 0
max_auto_fix: 0

# O comando `ft run . --template feature` sempre cria uma worktree externa. Este arquivo é
# resolvido a partir do processo local `.ft/process/feature/`, nunca do template
# global.
