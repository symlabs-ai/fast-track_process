run_mode: isolated
max_gate_retries: 0
max_auto_fix: 0

# O comando `ft run . --template material_design_pwa` sempre cria uma worktree
# externa. Este arquivo é resolvido a partir do processo local
# `.ft/process/material_design_pwa/`, nunca do template global.
